# Trip-Matrix
"Trip matrix is a python-Tkinter vehicle trip logging system that stores trip details such as vehicle number, driver name, start/end locations, distance, fuel usage, and data in an SQLite database .It allows users to add, view, delete, and export trip records to CSV through a clean GUI interface.
